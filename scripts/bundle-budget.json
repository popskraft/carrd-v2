{
  "_comment": "Size budgets (bytes) for generated dist assets. Guards against gross bundle-size regressions. Per-plugin default applies to dist/<slug>/<slug>.min.js unless overridden. Tune after a fresh `npm run build` if a legitimate change pushes a file over budget.",
  "default_plugin_max_bytes": 24000,
  "plugin_overrides": {
    "shopping-cart": 24000,
    "slider": 22000,
    "switcher": 12000,
    "modal": 12000
  },
  "bundles": {
    "theme-core.min.js": 100000,
    "theme-core-compat.min.js": 120000
  }
}
