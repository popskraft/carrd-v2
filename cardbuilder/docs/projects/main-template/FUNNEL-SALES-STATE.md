# Main Template Funnel Sales State

## Purpose

Current live Builder draft state for `main-template` after the self-selling funnel pass.

## Verified flow

- Hero: `A Carrd demo that sells the plugin system`
- Header CTA: `Get the template` → `#data-modal-contact`
- Section flow:
  - `#hero` → intro and positioning
  - `#why` → funnel explanation
  - `#pricing` / `#offer` → full demo vs lightweight clone
  - `#examples` → supporting proof blocks
  - `#compare` → switcher comparison
  - `#faq` → objections and reuse questions
  - `#details` → longer explanatory / legal text
  - `#contact` → lead capture form
  - `#footer` → closing block

## Offer copy

- Full demo pack: `Buy full demo pack`
- Lightweight clone: `Buy lightweight clone`
- Switcher labels:
  - `Full demo`
  - `Lite clone`
  - `Template network`

## Lead capture

- Form id: `lead-form`
- Form button: `Request access`
- Form subject: `Template access request`
- Form fields:
  - `lead-email` / `Email`
  - `lead-name` / `Name`
  - `project-brief` / `What do you want to build?`

## Notes

- The page intentionally stays long and explanatory because it is both a demo and a product page.
- The current state is live in Builder draft only; publish still needs operator confirmation.
