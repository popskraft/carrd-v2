# Carrd Source Reference

## Purpose
`/Users/popskraft/Projects/CARRD/carrd-source` is the reference template source for comparison and regression checks.

## Status Rule
The reference is not guaranteed to be current at every moment.
Use it as a comparison baseline, not as the single source of truth for live behavior.

## Operational Notes
- Do not treat `carrd-source` as the only source of truth for live behavior.
- If it disagrees with live/template state or canonical docs, prefer the current live state and the repository docs.
