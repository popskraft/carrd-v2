# Дорожная Карта

## Суть
`carrd-v2` перешёл на inline-embed-only доставку начиная с `2.0.0` (2026-07-11): CDN/jsDelivr-бандлы (`theme-runtime.min.*`, `theme-core.min.*`, все `*-cdn.html`) и legacy compatibility-слой (`theme-compat.css`, compat-вариант `theme-ui.css`) полностью удалены из build pipeline и `dist/`. Канонические site-level файлы — `theme-ui.css`/`theme-ui-embed.html` и `theme-design-tokens.css`/`theme-design-tokens-embed.html` (плюс объединённый `theme-design-system.html`), у каждого плагина — только `*-embed.html` (или split part1/part2). См. `CHANGELOG.md` [2.0.0] и `docs/specs/release-contract.md` для версионирования (`2.0.0` — отдельная линия, ниже `2.1.0` по номеру намеренно; будущий возврат CDN зарезервирован под `3.0.0`).

Важно: это repo-side изменение build pipeline. Уже опубликованные live-сайты (`main-template`/`mini.crd.co`, `faktura`) всё ещё физически используют старые `@2.1.0` CDN-embeds внутри Carrd Builder — они продолжат работать (jsDelivr отдаёт immutable `v2.1.0` вечно), но требуют отдельной operator-миграции на новые inline embeds из текущего `dist/`, если владелец решит их обновить. Это не сделано автоматически и не входит в этот repo-side коммит.

Package-manager cleanup update `2026-07-12`: active repo commands, AGENTS/runbooks и nested `cardbuilder/projects/faktura/automation` приведены к `pnpm`; `validate` дополнительно нормализован под `pnpm`-поведение, чтобы `knip`/`eslint` больше не получали лишний `--silent`.

## Текущий readiness status — 2026-07-11

Технический runtime и release pipeline проходят полный локальный `npm run validate`. Прямой live-check `2026-07-11` подтвердил, что Carrd Builder reachable/authenticated, но не снял launch blockers: published `mini.crd.co` всё ещё тянет `no-loadwaiting@b910f70`, прямой `quickPublish()` из Builder завершился `Publish Failed :(`, а пробный write/readback новых `data-*` attrs (`control03`, `container12`, `container11`) не пережил `location.reload()`. Для self-service продажи проект пока не готов: нужен рабочий publish path, закрытие Q015/Q022, buyer-facing install/support/legal/privacy пакет и воспроизводимый test purchase/delivery flow. Ограниченный owner-assisted pilot допустим только после ручного подтверждения этих P1-гейтов. Полный разбор: `docs/reports/2026-07-11-project-readiness-audit.md`.

Next step: восстановить рабочий operator-approved publish path для canonical `main-template`, добиться сохранения draft mutations через Carrd Builder, затем снова заменить `no-loadwaiting@b910f70` на `@2.1.0`, выполнить `npm run check:published`, browser smoke и закрыть Q015/Q022 до подготовки release handoff. Параллельно нужно решить install-default strategy: inline embeds by default для buyer path, CDN/jsDelivr только как advanced/specialist path. Shared bundles уже получили fault isolation для plugin initialization errors; детерминированная матрица live-сценариев описана в `cardbuilder/docs/projects/main-template/LIVE-SCENARIO-TESTING-PLAN.md`.

## Ядро
| # | Работа | Статус |
|---|---|---|
| 1 | Зафиксировать `docs/` как active durable-docs root | done |
| 2 | Нормализовать root `AGENTS.md`, `README.md`, `CLAUDE.md`, `DEFINITION-OF-DONE.md`, `OPEN-QUESTIONS.md` и `docs/INDEX.md` под текущий documentation canon | done |
| 3 | Создать `popskraft/carrd-v2` как отдельный runtime repo для второй версии | done |
| 4 | Формализовать v2 publication contract без публикации v2 в legacy `popskraft/carrd-plugins` | done |
| 5 | Перевести source/dist на `*-v2` public identity, `theme-core`, `Carrd*V2` globals и `popskraft/carrd-v2@main` snippets | done |
| 6 | Применить audit fixes: shopping-cart text escaping, slider ARIA region label, modal refresh API, FAQ rAF height adjustment, cookie Secure flag | done |
| 7 | Сохранить legacy `popskraft/carrd-plugins` историческим freeze ref перед публичным v2 rollout | done |
| 8 | Опубликовать validated `popskraft/carrd-v2` main/tag `v2.0.0` и purge только v2 jsDelivr paths | done |
| 9 | Перенести `admincarrd` и `cardbuilder` из legacy workspace в `carrd-v2` и обновить локальные связи | done |
| 10 | Проверить live-синхронизацию нескольких controllers с одинаковым `data-switcher` | active |
| 11 | Проверить live cluster-переключение целых containers через `data-switcher-cluster` — проверка отменена решением Q002 в пользу полного удаления cluster mode | done |
| 12 | Проверить `accordeon` на рабочем Carrd-сайте на группе `ppf` | active |
| 13 | Проверить promoted `header-nav` anti-jump mobile collapse на рабочем Carrd-сайте | done |
| 14 | Закрепить `cardbuilder` operational canon без внешних workspace-зависимостей | done |
| 15 | Перевести canonical `main-template` Builder `4155176224428477` на явные v2 embeds и v2-разметку; затем выполнить operator publish и post-publish verify | done |
| 16 | Довести `faktura` до canonical scan package и обновить Builder draft на v2 plugins | done |
| 17 | Расширить broken-link/legacy-root проверку с operational canon на критические `cardbuilder/docs/` indexes и runbooks | active |
| 18 | Закрепить per-site Chrome-профили: добавить `chromeProfileDir` в `sites.json`, перевести `open-debug-chrome.sh` на чтение из реестра, задокументировать в quickstart | done |
| 19 | Добавить `CARRD_KNOWLEDGE_SOURCES.md` — инструкция по доступу к docs-rag-mvp RAG-сервису, ссылка из `cardbuilder/AGENTS.md` и `shared/INDEX.md` | done |
| 20 | После operator publish для `faktura` снять post-publish scan и подтвердить published runtime v2 plugins — закрыто live verify `faktura-dev.crd.co` в строке 26 | done |
| 21 | Добавить backward-compat слои во все 9 interactive plugin JS: поддержка v1-атрибутов и `CarrdPluginOptions` fallback | done |
| 22 | Заменить все plugin embeds в faktura draft на свежие v2 (backward-compat) и перевести `embed10` config на `CarrdPluginOptions`; publish остаётся operator-only | done |
| 23 | Полный readiness-аудит v2 (4 параллельные проверки + adversarial verify): расширить config-fallback на typography/header-nav/no-loadwaiting/shopping-cart, legacy data-attrs + `CarrdShoppingCart` alias для shopping-cart, modal lazy `[data-modal]` lookup | done |
| 24 | Добавить backward-compat тесты (`tests-js/backward-compat.test.js`, helper `setLegacyPluginOptions`): v1-атрибуты + v1-config + V2-precedence, 13 тестов | done |
| 25 | Закрыть container-scoped CSS alias-гэп: faq/cookie/modal (`1ac3dd4`) + найденный switcher гэп (`eca34b3`); systematic-скан подтвердил отсутствие других tails; пересобрать dist (`ab0eee6`); validate 189/189 | done |
| 26 | Синхронизировать faktura Builder с alias-fixed dist (`automation/sync-v2-embeds.mjs`, embed02/03/07, hash-readback 3/3); опубликовано оператором; live verify `faktura-dev.crd.co` подтверждает fixed FAQ/modal/cookie scope | done |
| 27 | Провести полный аудит remote-control поверхности Carrd Builder `4155176224428477`, устранить false-positive readiness, обновить live knowledge package и закрыть отсутствующие element-type fixtures | done |
| 28 | Собрать минимальный Carrd Builder MCP v1: deterministic control core, thin stdio MCP adapter, per-profile target maps, live sync/read/write safety и green validation | done |
| 29 | Снять публичный suffix `-v2` с plugin naming, source/dist docs, theme contract и public `data-*` markers, сохранив `popskraft/carrd-v2` runtime path и backward-compat aliases | done |
| 30 | Выполнить clean-runtime plan: Stage 1 repo cleanup + Stage 2 test/remediation завершены; clean runtime подтверждён (`npm run validate`, 187/187 JS/unit/integration tests, clean-contract green) | done |
| 31 | Продолжить миграционный этап: `cardbuilder/main-template` синхронизирован; `faktura` опубликован и post-publish verified 2026-07-02 на current clean runtime (`embed08/02/03/04/06/05/07/13/09` + `embed10`), clean markup подтверждён для Modal/Cookie/FAQ/Floating CTA/Grid/Slider/Header Nav; следующий шаг — export/install в `faktura-app` | active |
| 32 | Синхронизировать `main-template` Builder draft `4155176224428477` с clean plugin contract: обновить CDN/embed paths без `*-v2`, заменить все live `data-*-v2` / FAQ helper text / modal hash, и добить automation fallback для `#data-modal-v2-contact` | done |
| 33 | Сделать core `cardbuilder` validation machine-independent: убрать hardcoded repo root из `tests-js/site-registry.test.js` и entrypoints `open-debug-chrome.sh` / `refresh-builder-plugins.mjs`, включить `test:cardbuilder` в default `npm run test` | done |
| 34 | Зафиксировать pre-release theme/custom contract в governance и public README: repo-owned delivery assets отдельно от site-owned token overrides, site CSS, `window.CarrdPluginOptions` и site custom JS | done |
| 35 | Переписать public `README.md` как короткий English end-user guide: убрать `Key Ideas`, внутренние repo details и оставить только install/custom/troubleshooting guidance | done |
| 36 | Унифицировать все plugin README: ввести минимальный source contract, deterministic template generation, bundle/add-on и placement-aware install blocks, пересобрать `dist/` и закрепить contract tests | done |
| 37 | Первым делом удалить switcher cluster mode: `main-template` draft `cases` уже переведён на `data-switcher-target` + indexes `1..3`, runtime/config/tests/docs удалены и `npm run validate` зелёный; published `mini.crd.co` всё ещё на старом cluster markup и ждёт operator publish + post-publish live verify | active |
| 38 | Закрыть Q003: удалить неиспользуемый `data-shopping-cart-target` из runtime config, active contracts и `main-template` automation; закрепить единый Section Break/form/output checkout contract | done |
| 39 | Закрепить Q005: считать `main-template` Builder + `mini.crd.co` единым canonical smoke surface; после строки 37 добавить недостающие fixtures всех 13 plugins, smoke-матрицу и fresh desktop/mobile inventory | active |
| 40 | Удалить obsolete migration workspace, его snapshots, docs и active references; перенести скрытую Playwright dependency в `faktura` automation | done |
| 41 | Восстановить `shopping-cart` checkout на нативной Carrd custom form: runtime снова ищет textarea `name="order-details"` внутри `#form-shopping-cart` как fallback после `data-shopping-cart-output`, `dist/` пересобран, draft `4155176224428477` синхронизирован свежим JS; published `mini.crd.co` ждёт operator publish | active |
| 42 | Исправить CSS-минификатор: сохранять compound selectors после `:is()` без ложного descendant-пробела, усилить reset margin для `floating-cta`, полностью пересобрать `dist/` и подтвердить `npm run validate` | done |
| 42 | Автоматический MCP-онбординг сайта: `onboard_site` tool + `npm run onboard:site` (инвентаризация с stability-check, детерминированный keygen из `mutation-catalog.json`, coverage/contract self-check, write probe c auto-enable `state-write`, идемпотентный drift re-run); CDP timeout + U+2028/29 escaping + lint coverage `cardbuilder/scripts`; live-прогоны main-template/faktura ждут оператора | active |
| 43 | Новый плагин `stacker` (scroll-stack containers по `data-stacker` + alias `data-stacked`): реализован, 14 jsdom-тестов, live-verified оператором на `mini.crd.co` через inline embed; rollout в version-pinned CDN path ещё не завершён и ждёт operator publish/purge решения | active |
| 44 | Удалить shared fallback token `--theme-card-bg-default` и связанный `cards.defaultCardBg`: background contract для `cards` теперь только container-driven или `data-cards-color*`; пересобрать `dist`, провалидировать, push и purge jsDelivr | done |
| 45 | Перевести theme token contract на named-colors/roles/components: `theme-design-tokens.css` переписан, canonical delivery разделён на `theme-runtime.*` + `theme-design-tokens-embed.html`, compatibility delivery сохранён как `theme-core.*` / `theme-ui.css`, shared UI и plugin CSS переведены на новые primitives/plugin defaults, install docs и token tests обновлены, `npm run validate` зелёный | done |
| 46 | Закрыть rollout audit по theme contract: main-template automation переведена на version-pinned `theme-runtime`, compatibility bridge и install contract усилены тестами, независимый repo audit повторно подтверждён зелёным `npm run validate` | done |
| 47 | Завершить ownership migration токенов: глобальный embed содержит только shared primitives, plugin defaults принадлежат CSS плагинов через `:where(:root)`, canonical fallbacks удалены, optional Shopping Cart изолирован, guardrails и Browser computed-style smoke зелёные | done |
| 48 | Финализировать quality-audit fixes (`_temp/quality-audit-2026-07-07-v2.md`): пересобрать `dist/` из refactored `src/`, вплести `check:bundle-budget` в `validate`, добавить `knip` dead-code gate (`check:deadcode`, чистый первый прогон) и поднять `no-unused-vars` до `error`; `npm run validate` зелёный (230/230 JS, 27/27 py, coverage 100%). Cohesion/coupling split крупных plugin-файлов (shopping-cart/slider/modal) осознанно отложен — нужно согласование owner перед структурными изменениями | done |
| 49 | Опубликовать theme token contract split live на `main-template`: закоммичен + запушен, VERSION/CHANGELOG приведены к тегу `v2.1.0`, jsDelivr-резолвинг подтверждён, Builder-драфт переведён с `theme-core@main` на `theme-runtime@2.1.0` (`embed14`/`embed02`) + добавлен Head-embed `embed03` с `theme-design-tokens-embed.html`, stacker canonical-миграция включена в тот же publish. Опубликовано и live-verified на `mini.crd.co` (все `--theme-*` токены резолвятся, 6 slider/3 switcher/3 stacker/1 modal без регрессий, все plugin globals загружены) | done |
| 50 | Привести add-on plugin embeds к контракту «один embed на плагин, CDN, version-pinned»: свалка `embed01` (инлайновый shopping-cart JS + cookie-banner на `@main`) удалена, заменена на `embed04` (Shopping Cart CDN) + `embed05` (Cookie Banner CDN), `no-loadwaiting` (`embed08`) допинан. Опубликовано и live-verified на `mini.crd.co` — ноль `@main`-ссылок, все 7 CDN-ресурсов на `@2.1.0`, без инлайн-дублей и двойной инициализации виджета корзины | done |
| 51 | Освежить `cardbuilder` operational truth для `main-template` после аудита 2026-07-07: `ELEMENT-TABS-MAP`, `knowledge-status.json`, `published-site-plugin-scan.json`, `live-plugin-inventory.json`, `mcp-targets.json` и `check:tabs-drift` должны совпасть с текущим Builder (`106` элементов, `6` embeds) и live `mini.crd.co` на CDN `@2.1.0` | done |
| 52 | Закрыть два оставшихся audit-хвоста 2026-07-07: GitHub Actions должен вызывать полный `pnpm run validate`, а `npm run cdn:purge` — purge version-pinned ref из `VERSION` с явным override для compatibility `@main` | done |
| 53 | Собрать итоговый full-project audit 2026-07-07: обновить benchmark-based quality audit под текущие gates (`coverage`, `knip`, CI parity, security headers), добавить consolidated report и синхронизировать `docs/INDEX.md` | done |
| 54 | Закрыть follow-up по quality audit 2026-07-07: добавить `test:coverage:cardbuilder` в `validate`, подключить `axe-core` smoke для modal/faq/accordeon/switcher, добавить JSDoc в `switcher`/`faq`/`header-nav`/`slider`, исправить switcher list semantics (`ul` без forced `role=\"group\"`), пересобрать `dist/` и подтвердить зелёный `npm run validate` | done |
| 55 | Сократить authored contract `header-nav`: канонический marker переименован в `.header-mobile-hide`, runtime/CSS/docs/tests/dist переведены, legacy `.header-mobile-el-collapsing` сохранён как backward-compat alias; активация по-прежнему происходит только при наличии marker’а внутри `#header` | done |
| 56 | Перейти на immutable release-систему для продажи Carrd-шаблонов: SemVer tags остаются навсегда, customer templates используют только `@X.Y.Z`, старый автодеплой удалён, release manual и gates закреплены в `docs/specs/release-contract.md` | done |
| 57 | Зафиксировать dev-правило для mutable `@main`: development Carrd drafts используют только `@main` + единый ручной `?rev=YYYYMMDD-XX`, а pre-sale validation обязана блокировать `@main` и `?rev` в финальной продаже | done |
| 58 | Добавить диагностический add-on `design-palette`: видимый embed строит compact swatch grid по текущим theme color tokens, показывает token names и resolved hex values, поддерживает inline/CDN install, API refresh и jsdom tests; `dist` пересобран и `npm run validate` зелёный | done |
| 59 | Исправить `design-palette`: живой `mini.crd.co` ничего не рисовал, потому что `design-palette-embed.html` вставили в hidden Head embed без target-div — палитра уходила fallback-хостом в конец `<body>`. Build-скрипт (`scripts/minify_plugins.py`) теперь дописывает `<div data-design-palette></div>` прямо в `design-palette-embed.html` (после `<script>`) и в `design-palette-cdn.html` (marked `Visible` блок), README обновлён; `dist` пересобран, `npm run validate` зелёный (239/239 JS). Публикация на живом `mini.crd.co` — на операторе: нужно ещё добавить сам target-div в Builder на месте текста «Палитра тут» и опубликовать | active |
| 60 | Заменить canonical `grid-cluster` новой dynamic span-системой: active plugin теперь использует `data-grid*` / `data-grid-cols*` / `data-grid-span*`, входит в основной bundle, а предыдущая width/class-версия убрана из active линии и архивирована в `docs/_archive/grid-cluster-v1-source/` | done |
| 61 | Собрать и опубликовать прототип `slider-v2` (native CSS scroll-snap, `src/slider-v2/`) по `docs/specs/slider-v2-plan.md`: `slider.js`/`slider.css`, `demo.html` (4 кейса), README (6 data-атрибутов, оба режима, edge-behavior центрирования), `tests-js/slider2.test.js`. Подключён в `dist/slider-v2/` как individual (не bundled) плагин параллельно с v1 `slider`; `slider-v2` добавлен в `SPLIT_EMBED_PLUGINS`. После live-debug `mini.crd.co` исправлены mobile swipe (`touch-action:auto`), Carrd flex-width (`min-width:0`, container `width:100%`) и desktop arrows: стрелки теперь сдвигают ленту ровно на один фактический slide pitch, быстрые клики накапливают полные шаги, disabled-state считается по реальным scroll boundaries. По решению владельца `free` стал default mode без обязательного атрибута, dots теперь default `on` в обоих режимах и скрываются только через `data-slider-dots="off"`; `center` остался явным opt-in. Quality-pass добавил ARIA-позицию каждому слайду и полную очистку pending scroll timers/listeners при `destroy()`. Slider-v2 suite: 20 тестов, `npm run validate` зелёный. Метрики размера прототипа остаются выше исходной цели — принятый риск, см. `OPEN-QUESTIONS.md` Q012. Ожидает установки обновлённого embed и финальной owner-оценки | active |
| 62 | Мигрировать JS-only опции на data-*-контракт по `docs/specs/plugin-options-data-migration-plan.md`: accordeon (`data-accordeon-default-open/-scroll/-scroll-behavior/-scroll-block`), modal (`data-modal-close-on-overlay/-close-on-escape/-show-close/-lock-scroll`), switcher (`data-switcher-default-index`), shopping-cart (`data-shopping-cart-checkout-target`) — все с приоритетом data-* > `window.CarrdPluginOptions` > дефолт, warn+fallback на невалидные значения. `docs/specs/plugin-data-contract.md` расширен. stacker и floating-cta по факту уже были data-*-first — правок не потребовалось; grid-cluster `options.enabled` оказался используемым — не удалён. После promotion `slider-v2 → slider` удалён stale JS-config из source/docs/faktura automation, добавлены contract guards, `npm run validate` проходит: `254/254` JS, `27/27` Cardbuilder, `37/37` Python, generated `dist`, dead-code, lint и budget зелёные | done |
| 63 | Добавить `slider-v2` опцию `data-slider-arrows-mobile="on"`: показывает prev/next стрелки на мобильных (≤736px), где раньше они были безусловно скрыты через CSS. Owner подтвердил (2026-07-11) механику slider-v2 в целом, принял split на 2 embed-части как есть (проверка `terser` дала ~20% сокращение, недостаточно чтобы убрать split), отложил `mode=start`. См. addendum в `docs/specs/slider-v2-plan.md` и `OPEN-QUESTIONS.md` Q012 | done |
| 64 | Промоушен `slider-v2` → основной `slider`, v1 → архив (владелец подтвердил 2026-07-11). Выполнено: v1 сохранён в `docs/_archive/slider-v1-source/`, `src/slider/` и `tests-js/slider.test.js` используют data-only native scroll-snap engine, отдельный `slider-v2` delivery удалён, `dist` пересобран и `npm run validate` проходит | done |
| 65 | Ремапнуть `main-template` под текущую live Builder структуру: обновить top-level container registry и docs на `container27/28/26/18/01`, переназначить `slider-card-4` на `container16`, добавить stacker/switcher/footer/cookie blocks и сверить live tree с новым canonical map | done |
| 66 | Зафиксировать текущую self-selling funnel версию `main-template` в docs после Builder-правок: hero → proof → offer → comparison → FAQ → contact, с `lead-form` и `#data-modal-contact` | done |
| 67 | Построить локальную deterministic browser lab для полного `carrd-source`: безопасные per-scenario `data-*` mutations, ранние `CarrdPluginOptions`, local `dist` routing, isolated Chrome, screenshots/result JSON и стартовая smoke-матрица | done |
| 68 | Расширить local plugin lab первой generated matrix: machine-readable contracts, deterministic single/pairwise generation, parallel isolated Chrome contexts, compact failure evidence и 320/320 browser scenarios | done |
| 69 | Добавить сложные пользовательские flows поверх matrix: modal/Escape, switcher, FAQ, cookie acceptance, mobile header, floating CTA scroll и Shopping Cart API; подтвердить 12/12 browser PASS | done |
| 70 | Починить canonical publish path для `main-template`: live Builder `4155176224428477` reachable/authenticated, но прямой `quickPublish()` 2026-07-11 завершился `Publish Failed :(`; нужен рабочий operator-approved publish/save path, который сохраняет draft mutations и даёт повторяемый post-publish readback | blocked |
| 71 | Устранить published release drift на canonical smoke surface: заменить `no-loadwaiting@b910f70` на immutable `@2.1.0`, затем добиться зелёного `npm run check:published` и обновить published inventory/evidence | blocked |
| 72 | Закрыть live Builder persistence gap для новых `data-*` attrs: доказать, что Carrd сохраняет `data-shopping-cart-checkout-target`, `data-modal-close-on-overlay`, `data-slider-arrows-mobile` без искажений на canonical surface и что значения переживают reload/readback/publish | blocked |
| 73 | Перевести buyer install strategy на `inline embeds only`: CDN/jsDelivr-доставка удалена полностью (не оставлена как advanced/specialist path). `bundle.config.json` очищен от `cdn_bundle`/`compat_bundle`, `scripts/minify_plugins.py` больше не генерирует `theme-runtime.min.*`, `theme-core.min.*` или любые `*-cdn.html`; `src/theme-compat.css` и compat-вариант `dist/theme-ui.css` удалены. Версия отмечена как `2.0.0` (отдельная линия от `2.1.0`, не продолжение SemVer) — см. `CHANGELOG.md` и `docs/specs/release-contract.md`. Возврат CDN зарезервирован под будущий `3.0.0` | done |
| 74 | Собрать buyer-facing install package: `README.md` переписан на единственный Inline Embed путь (theme-design-system.html + per-plugin `*-embed.html`/split parts); канонический полный runbook по rollback/smoke ещё не собран отдельным документом | active |
| 75 | Собрать launch package для продажи: определить included/excluded scope, install ownership, support/troubleshooting flow, privacy/legal/refund terms и buyer compatibility matrix; отдельно зафиксировать, что `shopping-cart` — client-side order UI/form serializer, а не полноценный ecommerce backend | active |
| 76 | Доказать full published browser smoke для canonical sales surface после закрытия drift: critical desktop/mobile journeys, console/network failures, asset refs, keyboard/touch, modal/cart/slider/header flows | active |
| 77 | Усилить non-runtime launch hygiene: добавить formal quality gate для `admincarrd/**` и убрать абсолютные локальные пути из active docs/knowledge package там, где они не требуются для локальной эксплуатации | active |
| 78 | Зафиксировать canonical migration contract для `arbitrary site -> miniGree` и подготовить `cardbuilder` к реализации через frozen live canon, dry-run-only planning, approval gates, persistence proof и no-false-pass verdict contract | active |
